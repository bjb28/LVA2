from lva2.settings.base import *

DEBUG = False

ALLOWED_HOSTS = ['*']

# Need to explicitly enable logging for production configurations
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['console'],
            'level': 'INFO',
        },
    },
}